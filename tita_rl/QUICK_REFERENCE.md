# 🎯 从头训练快速参考

## ✅ 当前状态

**训练模式**: 从头训练（使用深度特征）  
**进程 ID**: 45274  
**状态**: ✅ 正在运行  
**日志文件**: `training_from_scratch.log`

---

## 📊 配置总结

| 项目 | 值 | 说明 |
|------|-----|------|
| **观测维度** | **591** | 586 + 5 (深度特征) |
| **深度特征** | ✅ 启用 | 左/中/右/近/远 |
| **预训练模型** | ❌ 不使用 | 从零开始 |
| **总迭代次数** | **40,000** | 12k + 12k + 16k |
| **学习率** | **1e-3** | 标准学习率 |
| **预计时间** | **10-15 小时** | RTX 3060 |
| **检查点数量** | **80 个** | 每 500 次一个 |
| **磁盘占用** | **~2 GB** | 80 × 25MB |

---

## 🔍 监控命令

### 查看实时日志
```bash
tail -f training_from_scratch.log
```

### 查看训练进度（动态）
```bash
watch -n 5 "tail -20 training_from_scratch.log"
```

### 查看进程状态
```bash
ps aux | grep train_parkour | grep -v grep
```

### 查看 GPU 使用
```bash
watch -n 1 nvidia-smi
```

### 启动 TensorBoard
```bash
tensorboard --logdir=logs/tita_parkour --port=6006
# 浏览器打开: http://localhost:6006
```

---

## 📁 检查点位置

```
logs/tita_parkour/parkour_with_curriculum/<时间戳>/
├── model_500.pt      # 阶段1 早期
├── model_1000.pt
├── ...
├── model_12000.pt    # ← 阶段1 结束
├── model_12500.pt    # 阶段2 开始
├── ...
├── model_24000.pt    # ← 阶段2 结束
├── model_24500.pt    # 阶段3 开始
├── ...
└── model_40000.pt    # ← 最终模型
```

---

## 📈 预期进度

### ⏰ 时间线

```
0:00  - 开始训练
0:10  - 环境初始化完成
0:30  - 第一批数据
1:00  - 第 1000 次迭代
3:00  - 第 12000 次（阶段1完成）✅
6:00  - 第 24000 次（阶段2完成）✅
10:00 - 第 40000 次（训练完成）🎉
```

### 📊 性能指标

| 阶段 | 迭代范围 | 目标奖励 | 碰撞率 | 重点 |
|------|---------|---------|--------|------|
| 1 | 0-12k | -5 → 5 | >50% | 基础行走 |
| 2 | 12k-24k | 5 → 12 | 20% | 障碍跨越 |
| 3 | 24k-40k | 12 → 18+ | <5% | 完美跑酷 |

---

## 🛑 停止训练

### 优雅停止
```bash
pkill -f train_parkour
```

### 强制停止
```bash
pkill -9 -f train_parkour
```

### 验证已停止
```bash
ps aux | grep train_parkour
```

---

## 💾 检查点保存

训练会自动保存：
- ✅ 每 500 次迭代保存一次
- ✅ 位置：`logs/tita_parkour/parkour_with_curriculum/<时间戳>/model_*.pt`
- ✅ 总共 80 个检查点

---

## 🔄 如果需要回滚到微调模式

1. **停止当前训练**
```bash
pkill -f train_parkour
```

2. **修改配置**
```python
# configs/tita_parkour_config.py
num_observations = 586  # 去掉深度特征
resume = True
resume_path = '/home/bubble/桌面/tita/tita_rl/model_11700.pt'
max_iterations = 31000
learning_rate = 4e-4
```

3. **修改 parkour_robot.py**
```python
def compute_observations(self):
    super().compute_observations()
    # 不添加深度特征
```

4. **重新训练**
```bash
python train_parkour.py --task=tita_parkour --headless
```

---

## 📱 快速命令

### 查看最新 10 条日志
```bash
tail -10 training_from_scratch.log
```

### 查看特定关键词
```bash
grep "Reward\|Stage\|Iter" training_from_scratch.log | tail -20
```

### 统计训练进度
```bash
grep -c "Iter" training_from_scratch.log
```

### 查看错误信息
```bash
grep -i "error\|exception\|fail" training_from_scratch.log
```

---

## 🎯 关键差异：从头训练 vs 微调

| 对比项 | 从头训练（当前） | 微调（之前） |
|--------|----------------|-------------|
| 观测维度 | 591 (含深度) | 586 |
| 预训练 | ❌ 无 | ✅ model_11700.pt |
| 训练时间 | 10-15h | 4-8h |
| 迭代次数 | 40,000 | 31,000 |
| 学习率 | 1e-3 | 4e-4 |
| 初期性能 | 很差（随机） | 较好（有基础） |
| 最终性能 | 未知（理论更好） | 可预期 |
| 深度使用 | 直接感知 | 间接（奖励） |

---

## 📞 常见问题

### Q: 训练什么时候完成？
A: 约 10-15 小时（RTX 3060，40000 次迭代）

### Q: 如何知道训练进度？
A: 查看日志中的迭代次数，或使用 TensorBoard

### Q: 可以中途停止吗？
A: 可以！检查点已保存，可以从任何保存点恢复

### Q: 深度特征真的有用吗？
A: 训练完成后对比测试：
```bash
# 测试有深度
python test_parkour.py --checkpoint=40000

# 测试无深度（临时关闭）
# 修改 use_camera = False
python test_parkour.py --checkpoint=40000
```

### Q: 如果效果不好怎么办？
A: 回滚到微调模式，或调整课程学习参数

---

## 🎉 预祝成功！

从头训练是一个漫长但值得的过程。深度特征应该能显著提升跑酷性能！

**重要提示**：
- ✅ 保持电脑运行（不要休眠）
- ✅ 定期检查日志
- ✅ 监控 GPU 温度
- ✅ 确保磁盘空间充足

---

## 📝 训练日志示例

```
======================================================================
🎯 TITA 跑酷训练（带课程学习）
======================================================================

📦 创建训练环境...
✅ 环境创建成功！ 观测维度: 591 (含深度特征)

🧠 创建策略网络...
✅ 策略网络创建成功！

📚 初始化课程学习...
✅ 阶段1: 12000 iterations (基础行走)
✅ 阶段2: 12000 iterations (障碍跨越)
✅ 阶段3: 16000 iterations (完美跑酷)

🚀 开始训练...
Iter 0/40000 | Stage: 1 | Reward: -12.3 | Cost: 1.2
Iter 100/40000 | Stage: 1 | Reward: -8.5 | Cost: 0.9
...
Iter 12000/40000 | Stage: 1→2 切换 | Reward: 5.2 | Cost: 0.4
...
Iter 24000/40000 | Stage: 2→3 切换 | Reward: 12.8 | Cost: 0.3
...
Iter 40000/40000 | Stage: 3 | Reward: 18.5 | Cost: 0.2
✅ 训练完成！
```

Good luck! 🚀
